# certified-nft — Roadmap / TODO (handoff)

This file is a **self-contained handoff** so a fresh session can start the tasks below
without prior context. Read the "Orientation" section first.

Baseline: main `b51d3ca` (everything below builds on it). All gates green, headline
theorems axiom-free.

---

## Orientation (current state)

**What this repo is.** A Rocq/Coq-verified nftables compiler + optimizer + parser. The
pipeline, with its trust boundary:

```
.nft text
  └─(UNTRUSTED frontend)─► Syntax.chain        extracted/lexer.mll + parser.mly
                                                + nft_parse.ml + nft_lower.ml
  Syntax.chain (Coq AST)
  └─(VERIFIED)─► optimize_table_uncond ─► optimized Syntax.chain     Optimize_Uncond.v
  └─(VERIFIED)─► compile_chain ─► bytecode (list of instr)           Compile.v
  bytecode
  └─(UNTRUSTED render)─► `nft --debug=netlink` text                  extracted/codec.ml (to_netlink_text)
```

**The verified guarantees (all axiom-free; `Print Assumptions` = "Closed under the global context"):**
- `Correct.compile_chain_correct` — the bytecode VM filters every packet exactly as the DSL semantics says.
- `Optimize_Uncond.optimize_table_uncond_correct` / `…_compile_correct` — the optimizer (and the
  compiled bytecode of the optimized chain) preserves every packet's verdict, **for any input ruleset,
  with NO precondition** (no `rules_clean`, no freshness side-condition). `optimize_table_uncond c` is a
  function of `c` alone; freshness is internal (names minted past `seed_start`).

**Optimizer passes implemented & verified** (compose into `optimize_table`, shipped as `optimize_table_uncond`):
- base `optimize_chain` (dedup of repeated matches, dead-rule elimination, singleton-range→`cmp eq`) — `Optimize.v`
- `setsN`  : value → anonymous set            — `Optimize_Merge.v`
- `vmapN`  : value + differing verdict → vmap  — `Optimize_Vmap.v`  (merge gated on `body_vmap_safe`: no notrack/synproxy)
- `concatN`: two differing selectors → concat  — `Optimize_Concat.v`
- `concatK`: K(≥3)-dimensional concat           — `Optimize_ConcatK.v`  (TODO 1b)
- `mapn`   : value → data MAP (`meta mark set … map`) — `Optimize_Mapn.v`  (TODO 1a; FIRST `sd_maps` writer;
  correctness over the meta-STATE via `dsl_step`/`eval_rules_mut`, not just the verdict `eval_chain`.
  CAVEAT: emits the head-set-GUARDED form, NOT `nft -o`'s bare map — see the fidelity note below)
- composition + unconditional correctness       — `Optimize_Table.v`, `Optimize_Uncond.v`, seam helpers in `Optimize_Table_Inv.v`

**Data model.** `Semantics.v` `Record set_decls := { sd_sets; sd_vmaps; sd_maps }`. Per-datatype
validity predicates + constructors are in `Nftval.v`. `sd_maps` is now written by the `mapn` pass
(`Optimize_Mapn.v`, TODO 1a) — the merged rule carries a head set guard (the map keys) so the lookup
always hits, making the data-map merge provably state-preserving. (Fidelity caveat: `nft -o` emits a
BARE map with no head guard; matching that needs NFT_BREAK-on-map-miss in `body_writes`/`ILookupVal` —
see TODO 1a note below.)

**Tooling layout** (`proof/extracted/`, built with dune):
- `glue.ml` — the CLI (modes: `optimize`, `optsets`, `optdemo`). Entry the executable runs.
- `nftc.ml` / `nftc.mli` — reusable library API (compile/optimize/render + the `.nft` frontend).
- `codec.ml` — bytecode⇄`--debug=netlink` text (render is the untrusted step; differentially tested).
- `lexer.mll` / `parser.mly` / `nft_parse.ml` / `nft_lower.ml` — the `.nft` text frontend (untrusted glue).
- There is **NO actual netlink send** today — the tool only RENDERS `--debug=netlink` text.

**Build & gates** (run from `proof/`, after `eval $(opam env --switch=vst)`):
- build: `coq_makefile -f _CoqProject -o CoqMakefile && make -f CoqMakefile`  (or `make proofs`)
- `make glue`        — build the extracted OCaml CLI
- `make corpus`      — MUST stay **2532/2532 round-tripped, 0 mismatches** (vs upstream nft corpus)
- `make validate`    — 28/28 field_load vs live `nft`
- `make parse-test`  — parser round-trip checks
- `make semtest`     — compile & optimize preserve the DSL verdict on every packet
- `make difftest`    — byte-identical to real `nft`

**Hard invariants for ALL work below (do not violate):**
- No `Admitted`/`admit`/`Axiom`/`Parameter`/`Abort`; never weaken, delete, or comment out an existing
  theorem statement. Headline theorems stay axiom-free.
- All gates stay green after every change; **corpus stays 2532/2532, 0 mismatches**.
- `Red_*.v` / `*_Gaps.v` / `*_Realistic.v` are committed regression/honesty artifacts — do NOT delete them.
  `*_Gen.v` files are auto-generated by `make gen` (nft2coq) — do NOT hand-edit; regenerate.
- Commit incrementally (durable checkpoints). Do NOT push. End commit messages with
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Relevant memory: `~/.claude/projects/-home-yiyun-Projects-certified-nft/memory/` (esp. `goal-five-objectives.md`,
  `two-bytecode-render-formats.md`, `nft-parser-to-coq-pipeline.md`, `extracted-gitignore-pitfall.md`).

---

## The adversarial-verification requirement (applies to EVERY task below)

For each task, after the implementation work, **spawn a SEPARATE, independent, skeptical reviewer
agent** (do not let the implementer grade itself). Its mandate, every time, is to *verify or refute*:

1. **The extracted/shipped code actually runs the VERIFIED term** — e.g. the CLI/library invokes the
   extracted `optimize_table_uncond` / `compile_chain` / the new verified passes, NOT a hand-written
   OCaml re-implementation that silently diverges from the proof. Trace the call path from `main`/the
   public API down to the extracted Coq function. Flag any hand-OCaml that duplicates verified logic.
2. **No corners cut** — no `Admitted`/`admit`/`Axiom`; every gate green (corpus 2532/2532, validate 28/28,
   parse-test, semtest); headline theorems still axiom-free; no existing theorem weakened or deleted.
3. **Non-vacuity** — the new theorems are meaningful (the optimizer actually fires the new merge; the
   pipeline actually transforms input), not trivially true (e.g. `c' = c`, or an unsatisfiable premise).
4. **Honest trust boundary** — anything untrusted (renderer, parser, netlink send) is clearly labeled as
   such and differentially tested; claims in README/DEVELOPMENT/LIBRARY are not overstated.

The reviewer returns **PASS/FAIL with specific file:line defects**. On FAIL, the implementer fixes and
the reviewer re-checks before anything is merged. Prefer isolated git worktrees per task; merge only after
PASS + a self-run of all gates.

---

## Progress log (2026-06-29 session)

**TODO 3 — DONE** (adversarial review: PASS). `theories/Nft_Tactics.v` (predicates
`nft_accepts`/`nft_drops`/`nft_field_is`/…, notations, tactics `nft_eval`/`nft_decide`,
step lemmas), demos `Nft_Demo_Symbolic.v` / `Nft_Demo_Concrete.v` (re-express existing
proofs byte-equal + false-property rejection), guide `proof/CONFIG_PROOFS.md`.

**TODO 2 — DONE.** `extracted/nftc_cli.ml` (CLI: `compile`/`optimize`/`send`, bottoms out
in `Compile.compile_chain` / `Optimize_Uncond.optimize_table_uncond`); parser gained
`meta mark set` (`parser.mly`); `proof/e2e.sh` + `make e2e` (parse→optimize→compile→render
vs live nft, value→set merge, real rulesets, coverage probe); `extracted/nl_send.ml` +
`nl_stubs.c` + `make nl-send` — a REAL `NETLINK_NETFILTER` sender that transmits the
verified `compile_chain` output 1:1, round-trip-tested in a netns (rules land in the
kernel; `nft list ruleset` confirms).

**TODO 1 — 1b (N-concat) DONE with nft -o fidelity; 1a (mapN) shipped as a SOUND pass but
NOT nft -o byte-faithful (head-set-guarded form; documented, see below).**
- `theories/Optimize_Normalize.v` (NEW, axiom-free): `normalize_chain` rewrites every
  `MEq f v` → `MCmp f CEq v` (extensionally equal: identical `match_loadable` and
  `eval_matchcond_body`), proved `eval_chain`-preserving. `optimize_table_uncond` now runs
  it FIRST, so the value→set/concat/vmap merge recognisers (which match `MCmp _ CEq _`)
  FIRE on PARSER output (the frontend lowers `==` to `MEq`). Effect: the shipped verified
  optimizer now consolidates real `.nft` rulesets (e.g. three `ip saddr` rules → one
  `lookup set {…}`, matching `nft -o`), where before it was a no-op on parser output. Both
  headline theorems still `Closed under the global context`, statements unchanged.
- **1a (mapN data-value maps) — verified SOUND pass shipped; nft -o BYTE-fidelity NOT achieved
  (documented).** `theories/Optimize_Mapn.v` (NEW, axiom-free): folds adjacent
  `ip saddr A meta mark set M1` / `… B meta mark set M2` into ONE
  `ip saddr { A, B } meta mark set ip saddr map { A:M1, B:M2 }`.
    - Verdict side is trivial (all rules `Continue`, so `eval_chain`-invisible); the NON-vacuous
      content is `dsl_step_map_merge`: `dsl_step merged p = dsl_step orig2 (dsl_step orig1 p)` — the
      data map yields exactly the right `MKmark`. Supporting: `body_writes_merged`/`body_writes_orig`/
      `mapn_head_mem` (the lookup selects M1 vs M2 by key), over `dsl_step`/`eval_rules_mut` (state),
      not the verdict-only `eval_chain`.
    - FIRST pass to write `sd_maps` (alongside the head `sd_sets`). `is_orig_map` recognises the
      original shell via pure constructor matches (NOT `rule_eq_dec`, which would extract the per-char
      `String.get` destructor / ~42 MB blow-up); checks BOTH body shape and all end-fields (sound recogniser).
    - COMPOSED into `optimize_table` (between concatK and concatN); the gen theorem
      `optimize_table_correct_uncond_gen` threads the stage. Both headline theorems still
      `Closed under the global context`. Extracted; `make semtest` §(6f) runs the verified
      `optimize_chain_mapn` + `dsl_step` and shows the merged mark matches the two-rule composition
      on key hits (0x0a/0x14) and a miss (unchanged). Commits `a1278da` (impl), `<fidelity-doc>`.
    - **ADVERSARIAL REVIEW (1a-specific): formal core PASS, fidelity FAIL → corrected to honest framing.**
      Independent skeptic confirmed axiom-free / no-admits / non-vacuous / sound-recogniser / all gates
      green / shipped binary runs the extracted verified term — the theorem proved is TRUE. BUT, tested
      vs `nft v1.1.6`: (1) `nft -o` does NOT merge `meta mark set` at all; (2) for the maps it DOES emit
      (dnat/snat) it emits a BARE map with NO head guard. Our head-set guard is required for soundness
      *in our model* (statement value-maps load the lookup default on a miss rather than NFT_BREAK —
      `Semantics.v:696`/`:1241`, `Bytes.v:37`), so the guarded form is valid/equivalent nftables but is
      NOT nft -o's output. The original commit MISLABELLED the guarded form as the "nft -o oracle"; this
      was corrected — `Optimize_Mapn.v` header + `semtest.ml` §6f now state plainly that this is a SOUND
      data-map consolidation, NOT nft -o byte-fidelity.
    - **REMAINING to truly close the bare-map gap — IN PROGRESS on branch `dnat-map-miss`** (NOT on main).
      Targets `dnat to … map` / `snat to … map` (the case `nft -o` actually merges). Three phases:
      - **Phase 1 — NFT_BREAK-on-map-miss (DONE on branch, commit `ea2838f`; `compile_chain_correct`
        re-proved, "Closed").** `Bytes.map_has_key`; new `Bytecode.ILookupValBr` (breaking lookup for a
        TERMINAL map operand; non-breaking `ILookupVal` stays for verdict-neutral statement maps where a
        miss is verdict-equivalent); `Semantics.nat_map_key`; `terminal_loadable` nat_map case now requires
        the key present; `nat_addr` refactored; `ILookupValBr` breaks in `run_rule`/`run_rule_writes`;
        `Compile` nat_map → `ILookupValBr`; `Correct`: `run_map_nat`/`run_map_nat_break` with the
        register↔field-value key bridge (`run_transforms_key` via `run_transforms_at_prefix`),
        `run_terminal`/`run_terminal_break` threaded.
      - **Phase 2 — optimizer agreement+freshness (PARTIAL on branch, commit `df78c06`).** Because
        `terminal_loadable` now reads `e_map`, the env-agreement infra must track it.  DONE: `rule_nat_map_name`;
        `decls_agree_rule` += e_map clause; `terminal_loadable_env`/`tail_loadable_env`/`end_loadable_agree`
        conditional; `rule_loadable_agree_gen`/`outcome_agree_gen` threaded (`Optimize_Table_Inv` compiles).
        TODO (the green-Phase-2 blocker): `decls_agree_rule_setseam`/`_vmapseam`/`_sym` must discharge the
        e_map clause — trivial for the sd_maps-preserving passes (setsN/concatK/concatN/vmapN), but mapN/dnat
        need a `rule_nat_map_fresh` track mirroring `rule_set_fresh` (def + mono + ~8 `output_fresh` lemmas
        [all trivial — NO pass mints a nat_map name] + `seed_start` + gen-theorem threading). Until this
        lands, `optimize_table_uncond_correct` does not compile on the branch.
      - **Phase 3 — the dnat/snat merge pass itself + NAT-effect correctness** (not started): recognise
        adjacent `field=A dnat to T1` / `field=B dnat to T2`, emit the BARE `dnat to field map {A:T1,B:T2}`
        (writing `sd_maps`, NO head guard — sound now that miss breaks); prove verdict-preserving over
        `eval_chain` AND the NAT packet rewrite (`apply_nat`) over a state-exposing evaluation (analogue of
        `dsl_step_map_merge`, for `r_nat`).
      Estimated remaining: Phase 2 ~15 mechanical lemmas; Phase 3 comparable to the whole mapN effort.
- **DONE — 1b (N-dim concat).** The N(≥3)-field concat pass is implemented, proved axiom-free,
  COMPOSED into the shipped `optimize_table_uncond`, extracted, and semtested. The shipped
  optimizer now synthesises ≥3-field concatenation sets (e.g. two `ip saddr . ip daddr . ip
  protocol` rules → one `lookup` over a 2-element 3-field concat set), matching `nft -o`.
  `theories/Optimize_ConcatK.v`: `packN`/`concat_in_iv_pointsN` (membership cert),
  `concat_fields_certificate_N` (matchcond cert), `eval_rules_concat_mergeK` (merge),
  `concat_mergeK_pair` (recogniser), `optimize_rules_concatK`/`optimize_chain_concatK` (pass) +
  seam bookkeeping (assoc_stable/vmaps/mono/keys_bound). `theories/Optimize_Uncond.v`:
  `optimize_rules_concatK_correct_uncond` + output-fresh/chain wrappers/`fresh_setname`;
  `optimize_table` (Optimize_Table.v) gained the stage (setsN→concatK→concatN→vmapN);
  `optimize_table_correct_uncond_gen` re-proved threading it. semtest §(6c-N) + e2e check B2
  demonstrate it fires + `eval_chain` agrees. GATE (same as the 2-field pass): all concat fields
  must be fixed-width PAYLOAD (`field_fixed_len = Some`); a non-fixed-width meta dependency (the
  `l4proto` dep of `tcp dport`) is not folded into the key — a faithful refinement still open.
  (Historical: the verdict-preservation core lemmas, axiom-free —)
  - `packN` (pack K field values, each in its `reg_slot`, last takes remainder; `packN [a;b] =
    pack2 a b`) + membership certificate `concat_in_iv_pointsN` (a packed point key is matched
    iff every field equals its stored value; general-branch forallb-over-split proved directly,
    `concat_match_packN`, sidestepping `concat_in_iv`'s singleton dispatch);
  - matchcond certificate `concat_fields_certificate_N` (`MConcatSet fields …` = existsb over
    rows of the per-field `MCmp f_i CEq a_i` conjunction);
  - **the merge theorem `eval_rules_concat_mergeK`** — `merged_ruleK` (one `MConcatSet` head atop
    `body`) provably replaces a RUN of `orig_ruleK` rows (K head matches each), verdict-preserving
    on every packet, via `eval_rules_run_collapse` (the K-match prefix is transparent to
    synproxy/notrack/thread, so all rows share one loadability+outcome; the merged applicability
    is the existsb the certificate gives).
  - **Remaining (the pass plumbing around the proven core):** the recogniser (`head_value2`→a
    K-prefix extractor + a run collector mirroring `take_concat_run`), the executable
    `optimize_rules_concatKN` fixpoint, and the `Optimize_Uncond` composition (mirror the
    `concatN` section: `_correct_uncond` / `_assoc_stable` / `_output_set_fresh` /
    `_output_vmap_fresh` / `_vmaps` / `_mono`, then add the stage to `optimize_table` and re-prove
    `optimize_table_correct_uncond_gen`) + `Extract.v` wiring + a semtest battery. Large but it
    mirrors the existing `concatN` plumbing 1:1.

---

## TODO 1 — Close the gap between our optimizer and `nft -o`

**Goal.** Make the optimizer a faithful superset of upstream `nft --optimize`. Two capabilities are
missing today (the three core consolidations — value→set, value+verdict→vmap, 2-field concat — are done).

### 1a. Data / value maps (the `sd_maps` pass)
`nft -o` folds rules whose differing part is a **statement value** (not the verdict) into a map keyed by
a match, e.g.
```
ip daddr A dnat to X
ip daddr B dnat to Y   =>   dnat to ip daddr map { A : X, B : Y }
```
and likewise `meta mark set … map { … }`. The model already has `set_decls.sd_maps`, but **no pass writes
it**. Implement a `mapN` pass (mirror `setsN`/`vmapN` in structure):
- Add the merge logic + a `body_map_safe`-style gate analogous to `body_vmap_safe` (watch for the same
  conntrack/notrack soundness pitfall the vmap pass hit — the merged rule reads its key relative to the
  body; if a ct-dependent key is read after a `notrack` latch it diverges. See `Optimize_Vmap.v`'s
  `body_vmap_safe` + `eval_rules_vmap_mergeN` for the pattern and why the gate is load-bearing).
- Prove an **unconditional** per-pass correctness theorem (`…_uncond`), matching the `Optimize_Uncond.v`
  style (read-freshness via `seed_start`, NO `rules_clean`). Compose it into the pipeline so
  `optimize_table_uncond_correct` still holds with the new stage.
- Wire it into `Extract.v` + `glue.ml` so the shipped optimizer runs it.

### 1b. N-dimensional concatenations (≥3 selectors)
`concatN` is hard-limited to exactly two selectors (`head_value2` matches `MCmp f1 :: MCmp f2 :: rest`).
Generalize to an N-tuple concat (`ip saddr . ip daddr . tcp dport { … }`):
- Generalize `head_value2` → an N-prefix extractor; generalize `concat_merge_pair` and the merge.
- Prove the unconditional per-pass correctness and compose into `optimize_table_uncond`.

**Per-pass discipline (both 1a and 1b):** every new pass needs (i) the function, (ii) an axiom-free
unconditional `_correct` theorem, (iii) extraction wiring so the SHIPPED optimizer uses it, (iv) a
`semtest` battery demonstrating it fires (rules shrink, the map/concat is synthesised) AND that
`eval_chain` agrees with the original on representative packets.

**Adversarial verification for TODO 1:** the reviewer must confirm the new passes' correctness theorems
are unconditional + axiom-free, the merge gates are real (not vacuous — find a notrack/ct case that the
gate must block), the SHIPPED `optimize_table_uncond` actually invokes the new passes (trace
`glue.ml` → extracted term), and corpus/semtest stay green. Cross-check a few outputs against real
`nft -o` (`difftest`-style) to confirm we match upstream behavior where claimed.

---

## TODO 2 — Tooling: real executable, complete parser, working end-to-end pipeline, netlink send

**Goal.** A user can take a `.nft` file and get verified, optimized rules onto the kernel — with the
trusted core being the extracted verified term and the untrusted edges clearly delimited + tested.

### 2a. Verified optimizer as a first-class executable
Today `glue.ml` is demo-oriented (`optimize`/`optsets`/`optdemo`). Make a clean CLI (e.g. `nftc`):
`nftc optimize FILE.nft` → parse → `optimize_table_uncond` → render; `nftc compile FILE.nft` → bytecode;
sensible flags, stdin/stdout, exit codes. The optimize/compile core MUST be the extracted verified
functions.

### 2b. Complete the parser
`lexer.mll` + `parser.mly` + `nft_lower.ml` cover a large subset but not all of nftables. Inventory what's
unsupported (grammar gaps, statement/expression kinds, address families) and extend coverage. Keep the
`parse-test` + corpus round-trip green as you grow it. The parser is UNTRUSTED glue (it produces
`Syntax.chain` terms the proof reasons about) — so its correctness is established by differential testing
against live `nft`, not by proof; keep that boundary honest.

### 2c. End-to-end pipeline that actually works
Make `.nft → parse → optimize → compile → (render | send)` work as one command, on real rulesets
(e.g. the repo's `router.nft` / `optiplex.nft`). Add an end-to-end test that runs a real `.nft` through
the whole chain and checks the result against live `nft` where possible.

### 2d. Actually send netlink messages
Today the tool only RENDERS `--debug=netlink` text. Add a real sender: open an `AF_NETLINK` /
`NETLINK_NETFILTER` (nfnetlink) socket and submit the compiled ruleset to the kernel (a thin libmnl-style
or hand-rolled nfnetlink batch). **Critical:** the bytes/messages sent MUST be exactly what the verified
`compile_chain` produced — the sender is untrusted transport, it must NOT re-derive or re-encode rules in
a way that bypasses the verified compile. Gate it behind an explicit flag (it mutates kernel state); make
it testable in a netns. Note the two byte-render formats (`codec.ml` corpus big-endian vs the
`--debug=netlink` little-endian 32-bit words) — see memory `two-bytecode-render-formats.md`; don't mix them.

**Adversarial verification for TODO 2:** the reviewer must (1) trace that `nftc`'s compile/optimize path
bottoms out in the extracted verified functions (no shadow OCaml optimizer/compiler); (2) confirm the
netlink sender transmits exactly the verified `compile_chain` output (diff the sent bytes against the
verified term's output / against `nft --debug=netlink` for the same ruleset); (3) verify the
trusted-vs-untrusted boundary is documented accurately (parser, renderer, sender are untrusted +
differentially tested; compile/optimize are verified); (4) all gates green; (5) the end-to-end test
actually exercises the real pipeline (not a stub).

---

## TODO 3 — Ergonomics for per-configuration theorems (readability + tactics)

**Goal.** Make it pleasant to STATE and PROVE properties about a concrete `.nft` ruleset. Today, proofs
like those in `Optiplex_Antispoof.v` / `Router_*.v` are written by hand against the parser-generated
`*_Gen.v` chains, with verbose `cbn`/`unfold`/`vm_compute` boilerplate and ad-hoc wire-byte literals.

- **Readable notations / definitions** for common configuration facts: "rule R drops packet P",
  "chain C accepts P", "field F of P equals V", verdict outcomes, conntrack/fib/nat predicates — so a
  property reads close to the nftables intent rather than raw byte lists. Route literals through the
  central `Nftval.v` constructors (validity-checked datatypes) instead of bare bytes.
- **Custom tactics** (Ltac/Ltac2) that discharge the recurring shapes: evaluating a rule/chain on a
  concrete packet, proving a match fires / does not fire, threading a chain trace rule-by-rule
  (`eval_chain_trace`), reducing `field_value`/`read_payload` to literals. Aim to turn today's
  multi-line `cbn -[…]; unfold …; rewrite …; vm_compute` rituals into one or two named tactics.
- A small **"how to prove things about your ruleset"** guide (in `proof/DEVELOPMENT.md` or a new doc),
  with a worked example: take a `.nft`, `make gen`, state a property with the new notations, prove it with
  the new tactics.
- Keep it ADDITIVE: existing theorems must still compile unchanged (or be re-expressed equivalently,
  byte-equal). This is a library/tactic layer, not a semantics change.

**Adversarial verification for TODO 3:** the reviewer must confirm the new notations/tactics are SOUND —
they don't silently change what's being proved (e.g. a notation that unfolds to a weaker statement, or a
tactic that closes a goal via a hidden `admit`/`Axiom`/over-eager `vm_compute` that masks a real
obligation). Concretely: re-express ≥1 existing hand-proof using the new layer and show it proves the
SAME statement (axiom-free, `Print Assumptions` Closed); attempt to use the tactics to "prove" a
deliberately FALSE configuration property and confirm they fail rather than succeed. All gates green.

---

### Suggested order
TODO 3 first (it makes everything else easier to state/verify), then TODO 1 (pure proof extension), then
TODO 2 (tooling + the untrusted netlink edge). Each is independent; do them in isolated worktrees.
