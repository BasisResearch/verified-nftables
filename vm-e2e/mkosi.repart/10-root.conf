# Root partition: the whole populated image tree, minimally sized.
# ext4 (read-write) so the booted VM can create files under /root and /tmp for
# the e2e battery.  Minimize=guess shrinks it to just fit the contents.
[Partition]
Type=root
Format=ext4
CopyFiles=/
Minimize=guess
